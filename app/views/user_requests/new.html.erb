<div class="page-header">
  <h1><%=t '.title', :default => @artist.name %></h1>
  <h3><%=t '.title', :default => "what would you like them to say or quickly record?" %></h3>
</div>

<%= render :partial => 'form' %>
